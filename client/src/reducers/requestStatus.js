// import * as types from '../constants/actionTypes';
import initialState from './initialState';

export default function requestStatusReducer(state = initialState.requestStatus, action) {
  switch(action.type) {
    default:
      return state;
  }
}
